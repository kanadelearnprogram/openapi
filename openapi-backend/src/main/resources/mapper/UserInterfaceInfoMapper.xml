<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openapi.project.mapper.UserInterfaceInfoMapper">

    <resultMap id="BaseResultMap" type="org.openpai.common.model.entity.UserInterfaceInfo">
            <id property="id" column="id" />
            <result property="userId" column="userId" />
            <result property="interfaceInfoId" column="InterfaceInfoId" />
            <result property="totalNum" column="totalNum" />
            <result property="leftNum" column="leftNum" />
            <result property="status" column="status" />
            <result property="isDelete" column="isDelete" />
            <result property="createTime" column="createTime" />
            <result property="updateTime" column="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id,userId,InterfaceInfoId,totalNum,leftNum,status,
        isDelete,createTime,updateTime
    </sql>
    <select id="listTopInvokeInterfaceInfo" resultType="org.openpai.common.model.entity.UserInterfaceInfo">
        SELECT InterfaceInfoId, SUM(totalNum) AS totalNum FROM user_interface_info
        GROUP BY InterfaceInfoId ORDER BY totalNum DESC LIMIT #{limit};
    </select>
    <select id="listInterfaceNum" resultType="com.openapi.project.model.vo.InterfaceNumVO">
        SELECT
            i.id as id,
            i.name AS interfaceName,
            i.description AS des,
            COALESCE(uii.leftNum, 0) AS leftNum
        FROM
            interface_info i
                LEFT JOIN
            user_interface_info uii
            ON
                i.id = uii.InterfaceInfoId AND uii.userId = #{userId}
        WHERE
            i.isDelete = 0;
    </select>
</mapper>
